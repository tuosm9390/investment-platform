## 1. 환경 설정 및 분석

- [x] 1.1 기존 뉴스 수집 파일(`src/lib/crawler.ts`) 분석 및 사용처 확인
- [x] 1.2 investing.com의 뉴스 페이지(예: 암호화폐/주식 종목별 뉴스 URL) 구조 파악 및 CSS 셀렉터 확인
- [x] 1.3 필요한 패키지(cheerio 등)가 `package.json`에 없으면 추가

## 2. investing.com 크롤러 구현

- [x] 2.1 `src/lib/crawler.ts` (또는 신규 모듈)에 `fetchInvestingNews` 함수 기본 골격 작성
- [x] 2.2 안티봇 우회를 위한 User-Agent 헤더 및 Request Delay 로직 적용
- [x] 2.3 HTTP GET 요청 및 Cheerio를 이용한 뉴스 제목, 링크, 작성일, 요약 데이터 파싱 로직 구현
- [x] 2.4 파싱 중 에러 발생 시 빈 배열 반환 및 에러 로깅 처리 (Fallback)

## 3. 데이터 통합 및 기존 로직 교체

- [x] 3.1 크롤링된 데이터를 애플리케이션 내부 뉴스 포맷에 맞게 변환하는 로직 추가
- [x] 3.2 기존의 단순 키워드 매칭(제목에 종목명 포함 여부 확인) 뉴스 수집 로직 제거
- [x] 3.3 기존 뉴스 데이터를 요청하던 API/서비스에서 새 로직(`fetchInvestingNews`)을 호출하도록 연결 수정

## 4. 테스트 및 검증

- [x] 4.1 로컬 환경에서 특정 종목(예: 비트코인, 테슬라 등)을 대상으로 크롤러 동작 테스트
- [x] 4.2 수집된 뉴스가 실제 종목과 관련된 뉴스인지 데이터 품질 검증
- [x] 4.3 에러 핸들링 로직(가짜 URL 또는 DOM 변경 시뮬레이션) 동작 확인